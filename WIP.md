# WIP: 復帰時のルール行の取り直しを、可視行駆動へ置き換える

Issue #101 / v1.40 リリース前に入れる。

## 目的

PR #97 で `onResume` の `updateRows()` が `includeRules = false` になり、復帰時に録画ルール行を
取り直さなくなった。ルールが1125件ある環境では全件の取り直しに1分近くかかるため必要な変更
だったが、代償として録り終えた番組がルール行に現れなくなった。

「復帰したら、見ている行が新しくなっている」体験を、**ルール総数と無関係なコスト**で取り戻す。

## 実装したこと

- **トリガーは可視化**。`VerticalGridView` に `addOnChildAttachStateChangeListener` を張り、
  子ビューの attach/detach を「視界に入った／出た」の信号として使う
- **掃き出しは1本の Runnable をスロットル**（`mVisibleSweepScheduled`）。デバウンスにすると
  D-pad 長押しで行が流れている間に一度も発火しない
- **行ごとのクールダウン**（`mRuleRowRefreshedAt`、60秒）。1回の sweep で飛ぶのは可視行数が上限
- **初回ロードの時刻も同じ控えに入れる**（`updateContentsListRowWithCategory` の先頭）。
  起動直後の sweep が同じ問い合わせを二重に出さないため
- **復帰時は sweep を1回呼ぶ**。行が既に attach 済みで attach イベントが来ないため
- **`getRules` は取り直さない**。ruleId は headerId から戻し、タイトルは行が持っている
- **並び順は動かさない**

## 決めたこと

- 初期ロードには適用しない。「0件ルール非表示」は各ルールの件数が要り、「録画の新しい順」は
  全ルールの最終録画日時が要るため、視界内だけでは成立しない
- `ruleIdFromHeaderId` の「1カテゴリ 10000 件まで」という前提は、既存の採番
  （`category.ordinal * 10000 + idInCategory`）がそのまま置いているものに合わせた

## 残タスク

- [ ] 実機確認
- [ ] WIP.md を削除してコミットしてから PR 作成

## 実機確認の観点

- 再生や詳細から戻ったとき、見えているルール行の内容が更新されること
- 前面で放置したあとスクロールして、現れた行が更新されること
- D-pad 長押しで高速にスクロールしても、リクエストが溜まらないこと（logcat の「可視行の取り直し」）
- 同じ行を何度も跨いでも再問い合わせが起きないこと（クールダウン）
- 起動直後の sweep が初期ロードと同じ問い合わせを二重に出さないこと
- 更新でカードが増減したとき、フォーカスが別の行へ飛ばないこと
- ルールが多い環境で、復帰が「読み込み中」で固まらないこと
