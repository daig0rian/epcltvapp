# WIP: CI に検証をさせる (`chore/ci-quality-gates`)

## 目的

単体テストが CI のどこでも走っていなかった。ワークフローは `release.yml` の1本だけで、
トリガーはタグ push のみ、実行するのは `./gradlew assembleRelease` だけ。
`assembleRelease` は `test` 系タスクに依存しないため、テストはタスクグラフに入りもしない。

あわせて、アプリ内アップデート機能によって**タグ時にしか検証できない整合性**が生まれた。
それも CI で見る。

## 入れるもの

### ① PR / master push で単体テスト (`test.yml` 新規)

壊れたものがマージされるのを防ぐ。JVM 上で完結するのでエミュレータもネイティブビルドも不要。

### ② タグ固有の整合性チェック (`release.yml` に追加)

**アプリは自分の `versionName` を GitHub のタグ名と比べて更新の有無を判断する。**
この2つは別の場所にある独立した値で、これまで一致を誰も検証していなかった。

ずれたときの事故: `versionName "1.37"` のまま `v1.38` のタグを打つと、公開APKは自分を 1.37 と
名乗る → 利用者が確認すると「v1.38 に更新」と出る → 同じAPKを入れ直しても 1.37 のまま →
**全利用者が永久に更新を促され続ける**。リリース自体は成功して見えるので気づけない。

チェック内容:
- タグ名 == `v` + `app/build.gradle` の `versionName`（最重要）
- `release-notes/<タグ名>.md` が存在する（今は最後のRelease作成ステップまで進んでから失敗する）
- `assembleRelease` の前に `testReleaseUnitTest`

順序は「重いツールチェーンを入れる前に、数秒で終わるチェックで落とす」。

## 完了済み

（なし）

## 残タスク

- [ ] `.github/workflows/test.yml` を追加
- [ ] `.github/workflows/release.yml` に検証ステップを追加
- [ ] CLAUDE.md のリリースフローに「タグと versionName が一致していないと CI が落ちる」旨を追記
- [ ] 動作確認（PR を作れば ① が走る。② は次のリリースまで本番検証できないので、
      ローカルで同等のシェルを流して確認する）
- [ ] `WIP.md` を削除してコミット → PR 作成
