# WIP: 1話しかないシリーズでもエピソード一覧を出す

ブランチ: `feature/episode-list-single-episode`

## 目的

再生画面のエピソード一覧が、同じシリーズの録画が1本しかないときに一切表示されない。
そのため利用者は「他の回が無い」のか「シリーズ情報の取得に失敗した」のかを区別できない。
1本しかない場合も一覧を出し、**1本しかないことが分かる**ようにする。

## 変更方針

`PlaybackVideoFragment.buildEpisodeListRow()` の `playlist.entries.size < 2` で
行そのものを作っていなかったのをやめる。一覧は録画が1本以上あれば常に出す
（0本のときだけ出さない＝見せるカードが無いため）。

あわせて行の見出しを「他のエピソード」からシリーズ名に変える。1本しかないことは、
再生中オーバーレイの付いたカードが1枚だけ並ぶことで分かる（件数による文言の
出し分けはしない）。

シリーズ名は `SeriesTitleExtractor` が番組名から取り出したもの＝EPGStation の検索に
使ったキーワードそのもの。これまで `SeriesPlaylist.load()` のローカル変数で捨てられて
いたので、`SeriesPlaylist` のプロパティとして持たせて画面まで運ぶ。

## 完了済み

- `buildEpisodeListRow()` の出し分け変更（`entries.size < 2` → `entries.isEmpty()`）
- `SeriesPlaylist.seriesTitle` を追加し、行の見出しをシリーズ名にした
  - `build()` に引数追加。呼び出しは `load()` 内2箇所とテストの `playlistOf()`
  - シリーズ名が空のときの保険として `action_episode_list` を残した
    （`load()` は空なら null を返すので実際には通らない）
- `episodeListAdded` の古いコメント（「他のエピソード」ボタンの可否判定）を修正。
  この変数は二重追加の防止にしか使われていなかった

## 残タスク

- [ ] ユーザーによる実機動作確認
- [ ] WIP.md 削除 → コミット → PR 作成

## 動作確認の観点

- 同じシリーズの録画が1本だけの番組を再生 → 行が出て、カードは再生中の1枚のみ
- 複数話ある番組を再生 → 従来どおり全話並ぶ（退行が無いこと）
- 見出しがシリーズ名（話数・タグを落とした番組名）になっていること。
  話数表記の癖が違う番組をいくつか見て、削りすぎ・残しすぎが無いか確認する
- 1本だけの場合に一覧へフォーカスを降ろせること・戻れること
- 再生中カードを選んだときのトースト「この回を再生中です」が従来どおり出ること
