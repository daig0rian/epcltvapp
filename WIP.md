# WIP: アプリ内アップデート機能 (`feature/in-app-update`)

## 目的

ストア掲載をしていないため、更新のたびにユーザーが GitHub Releases から APK を落として
サイドロードし直す必要がある。この手間をアプリ内で完結させる。
設定行に「アップデートを確認」カードを1枚置き、押すと確認 → ダウンロード → インストールまで進む。

計画の全文: `C:\Users\daigo\.claude\plans\sunny-jingling-catmull.md`

## 重要な決定事項（設計を貫く2原則）

1. **ユーザーが明示的に操作しない限り外部へ通信しない。**
   起動時チェックも定期確認もしない。カードを押し、さらに接続確認ダイアログで
   既定でない選択肢（初期フォーカスは「キャンセル」）を選んだときだけ通信する。
   ユーザー層がプライバシーに敏感であるため。
2. **状態を持たない。** 確認結果を保存してカードの見た目を変えることはしない。
   一般的なアプリに例がなくユーザーのメンタルモデルと合わないため。
   毎回まっさらから始まり、キャンセルすれば何も残らない。

その帰結として、起動しただけで新版に気づく手段は用意しない（ユーザー合意済み）。

その他:
- 完全サイレント更新は狙わない。システムのインストール確認ダイアログは出る前提。
- コルーチンは導入しない。既存コードに合わせて Retrofit `enqueue` / OkHttp 非同期 Call で書く。

## 完了済み

- [x] ① GitHub API クライアント + バージョン比較（`AppVersion` / `githubcaller/`、単体テスト付き）
- [x] ② カード + 接続確認 + 確認結果の表示（`AppUpdateDialogFragment`）
- [x] ③ APK ダウンロード（進捗表示・中止、`AppUpdateDownloader`）
- [x] ④ インストール起動と「不明なアプリのインストール」権限誘導

実装は一通り揃った。②で一度止める計画だったが、ダイアログ1枚に全段が入る作りで
分割すると同じファイルを二度書くことになるため、まとめて仕上げてから確認を依頼する形にした。

## 残タスク

- [ ] `./gradlew testDebugUnitTest`（`AppVersionTest`）が通ること
- [ ] 実機確認（計画の「検証」節の1〜5）
  - 1. カードを押さない限り GitHub へ通信しないこと（最重要）
  - 2. `versionName` を一時的に `"1.36"` に下げた release ビルドで確認〜ダウンロード
  - 3. release キーストアがあればインストール成功まで
  - 4. `"1.37"` に戻して「最新です」表示
  - 5. Fire TV / Google TV Streamer の両方、権限画面の有無
- [ ] `WIP.md` を削除してコミット → PR 作成
