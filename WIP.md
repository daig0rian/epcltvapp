# WIP: シリーズ再生まわりの改善

ブランチ: `feature/series-related-rows-order`

## 目的

1. 再生画面のシリーズ関連トーストが短すぎて読めないのを直す
2. 詳細画面の関連動画2行について、見出しと並び順を意図が伝わるものにする
3. 再生画面のエピソード一覧で、今再生中の回が一目で分かるようにする

## 完了済み

### 再生画面のトースト (`PlaybackVideoFragment.kt`)

`showQuickToast()` は「アイコンで状態が分かるトグルボタン向け」に 1 秒で消す用途のもので、
最終話で次のエピソードを押したときの「シリーズの次の番組がありません」もこれを使っていたため
読み切れなかった。文章を読ませるメッセージ用に `showMessageToast()` (`Toast.LENGTH_LONG`) を
追加し、シリーズ関連の4メッセージをそちらへ移した。

- `series_no_next` (最終話で次へ) ← 依頼された箇所
- `series_not_available` / `series_no_playable_file` / `series_already_playing`
  ← 同じボタン・同じ一覧から出る同種のメッセージなので揃えた

トグル系 (字幕ON/OFF・スーパー・音声切替) は従来どおり `showQuickToast()` のまま。

### 詳細画面の関連動画 (`VideoDetailsFragment.kt` / `strings.xml`)

| 行 | 見出し (旧 → 新) | 並び順 |
|----|------------------|--------|
| 0 | `<シリーズ名>` (変更なし) | 新しい順 → **古い順に変更** (第1話から始まる) |
| 1 | `同じルールの録画` → `同じルールの最新の録画` | 新しい順 (変更なし) |

0行目の見出しに `(放送日順)` を添える案は、見た目が良くないとの判断で撤回した (見出しはシリーズ名のみ)。

並び順は EPGStation API の `reverse` (v1) / `isReverse` (v2) で切り替える。
両バージョンとも `true` で `startAt ASC` = 古い順であることをサーバー側ソースで確認済み
(v1: `RecordedDB.ts` `option.reverse ? 'asc' : 'desc'` / v2: `RecordedDB.ts`
`orderBy('recorded.startAt', isReverse ? 'ASC' : 'DESC')`)。

「続きを読み込む」アイテムは `reverse` / `isReverse` を引き継ぐ実装になっているので追加対応は不要。

### エピソード一覧の「再生中」表示 (`OriginalCardPresenter.kt`)

再生中の回のサムネイル全体を50%の黒で覆い、中央に白抜きで「再生中」を出す。

`OriginalCardPresenter` に `nowPlayingId: Long?` を追加し、セットされたIDと一致する
カードにだけ重ねる。再生画面のエピソード一覧は専用インスタンスを持っているので、
ホーム・検索・詳細の一覧は null のままで影響を受けない。

**重ね方:** `ImageCardView` の親 `BaseCardView` は子ビューを縦に積む
(`onLayout` が `currBottom += mainView.getMeasuredHeight()`) ため、重ねるビューを
足せない。サムネイルの `ViewOverlay` (API 18+) に `Drawable` を載せて、レイアウトに
手を入れずに画像の上へ描いている。bounds は `setMainImageDimensions()` で固定している
`CARD_WIDTH`/`CARD_HEIGHT` をそのまま使う (実測を待たなくてよい)。

カードは使い回されるので、`onBindViewHolder` で毎回 clear してから必要なときだけ add する。

## 残タスク

- ユーザーによる実機動作確認
- 確認が取れたら WIP.md を削除してコミット → PR 作成

## 動作確認したいこと

- 最終話の再生中に「次のエピソード」を押し、トーストが読める長さで出るか
- 詳細画面の1行目の見出しがシリーズ名のままで、左端が第1話 (最も古い回) か
- 詳細画面の2行目が「同じルールの最新の録画」になり、並びは今までどおり新しい順か
- 1行目を右端までスクロールして「続きを読み込む」が古い順のまま続くか
- 再生画面の「他のエピソード」で、今見ている回だけが暗くなり「再生中」と出るか
- 一覧を左右にスクロールしても、印が別のカードへ移ったり消えたりしないか (カードの使い回し確認)
- 一覧から別の回へ移った後、印が新しい回へ付け替わっているか
- ホーム・検索・詳細画面の一覧には印が出ていないか
