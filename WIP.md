# WIP: fix/live-playback-controls

## 目的

ライブ再生（mpegts直送）の再生コントロールから、意味を持たない要素を取り除く。

1. **一時停止ボタンを消す** — ライブに一時停止の需要がない。
   併せて、このボタンで露見していた「キャプションが画面中央に寄る」バグの原因も直す。
2. **経過時間/総時間を消す** — シークバーを隠しているのに時刻だけ残っていた。

対象は **mpegts直送のライブ（チャンネルカード単押し）のみ**。
HLSライブ（長押し）は現状維持（シークバー・時刻・一時停止とも残す）——ユーザー確認済み。

## 完了済み

- ライブmpegts直送で再生/一時停止ボタンを置かないようにした
- ライブmpegts直送で経過時間/総時間の行を隠すようにした
- キャプションの位置計算を `showActionCaption()` に集約し、テキストと位置が必ず一緒に
  更新されるようにした。キャプション行が出来た時点で今のフォーカスから初期表示も作る
- `hideSeekBar()` を `setupTransportRow()` に統合した

## 残タスク

- 実機での動作確認
  - ライブ（単押し）: 一時停止ボタンが無い / シークバー・時刻とも出ない /
    最初に出るボタン（字幕 or 録画）のキャプションが**そのボタンの真下**に出る
  - ライブ（単押し）: 「録画」ボタンを押して「録画中」に変わってもキャプションが真下のまま
  - ライブ（長押し=HLS）: 従来どおり一時停止・シークバー・時刻が出る
  - 録画済み再生: 再生/一時停止を切り替えてもキャプションが真下のまま（回帰確認）

## 重要な決定事項

### キャプションが中央に寄るバグの原因

`updateActionCaption()` はフォーカス変化時にだけ `translationX` を計算していたが、
ボタンのラベルが変わったときに呼ばれる `updateActionCaptionText()` は**テキストだけ**を
差し替えて位置を触っていなかった。

ライブmpegts直送ではシークバーを隠しているため、コントロール行の最初のフォーカスが
ボタン列に乗る。その最初のフォーカス確定は、キャプション行を挿入する
`setupTransportRow()` より前に起きることがあり、その場合 `updateActionCaption()` は
`actionCaptionView == null` で何もせずに帰る。したがって `translationX` は 0 のまま。
その後、再生開始で Leanback が 再生→一時停止 にアイコンを差し替えた通知を
`actionChangeObserver` が拾い、`updateActionCaptionText()` がテキストだけを入れる
→ **幅いっぱい中央寄せのまま文字が出る**＝画面中央に寄って見える。

一時停止ボタンを消すだけでは同じ経路が「録画」ボタン（押すと「録画中」にラベルが
変わる）で再発するため、位置計算そのものを直す。

### 一時停止ボタンの消し方

`PlaybackTransportControlGlue.onCreatePrimaryActions()` が `mPlayPauseAction` を
生成して追加している。super を呼ばなければ `mPlayPauseAction` は null のままになり、
Leanback 側は `updatePlaybackState()` で null チェック済みなので安全に消せる
（leanback-1.0.0 のソースで確認）。リモコンのメディアキーも紐づく Action が無くなるため
効かなくなるが、これは意図どおり。

### 時刻表示の消し方

`current_time` の親（`secondary_controls_dock` と時刻3つを含む RelativeLayout）を GONE に
する。secondary actions はこのアプリでは使っていないので行ごと畳んでよい。
併せて `hideSeekBar()` を `setupTransportRow()` に統合した——どちらも同じ
「コントロール行のビューができるのを待って直接いじる」処理で、待ち受けが二重になっていた。
