# WIP: 1話しかないシリーズでもエピソード一覧を出す

ブランチ: `feature/episode-list-single-episode`

## 目的

再生画面のエピソード一覧が、同じシリーズの録画が1本しかないときに一切表示されない。
そのため利用者は「他の回が無い」のか「シリーズ情報の取得に失敗した」のかを区別できない。
1本しかない場合も一覧を出し、**1本しかないことが分かる**ようにする。

## 変更方針

`PlaybackVideoFragment.buildEpisodeListRow()` の `playlist.entries.size < 2` で
行そのものを作っていなかったのをやめる。

- 一覧は録画が1本以上あれば常に出す（0本のときだけ出さない＝見せるカードが無いため）。
- 行ヘッダーの文言を、今見ている回**以外**の有無で切り替える。
  - 他の回がある: 「他のエピソード」（従来どおり）
  - 他の回が無い: 「他のエピソードはありません」（新設 `episode_list_only_this`）
- 今見ている回のカードには従来どおり「再生中」が重なるので、
  ヘッダー + 再生中カード1枚で「このシリーズはこれだけ」と読める。

「今見ている回以外」で判定するのは、検索結果に今見ている回が含まれないケース
（MAX_PAGES 打ち切り等）があり、件数だけで判定すると文言が実態とずれるため。

## 完了済み

- `buildEpisodeListRow()` の出し分け変更（`entries.size < 2` → `entries.isEmpty()`）
- ヘッダー文言の切り替えと文字列リソース `episode_list_only_this` 追加（en / ja）
- `episodeListAdded` の古いコメント（「他のエピソード」ボタンの可否判定）を修正。
  この変数は二重追加の防止にしか使われていなかった

## 残タスク

- [ ] ユーザーによる実機動作確認
- [ ] WIP.md 削除 → コミット → PR 作成

## 動作確認の観点

- 同じシリーズの録画が1本だけの番組を再生 → 一覧が出て、ヘッダーが
  「他のエピソードはありません」、カードは再生中の1枚のみ
- 複数話ある番組を再生 → 従来どおり「他のエピソード」で全話並ぶ（退行が無いこと）
- 1本だけの場合に一覧へフォーカスを降ろせること・戻れること
- 再生中カードを選んだときのトースト「この回を再生中です」が従来どおり出ること
