# WIP: 操作マニュアルに「番組表からの録画」の節を足す

Issue #108 / ブランチ `chore/manual-recorded-from-guide`

## 目的

PR #100 で追加された「番組表からの録画」の行だけ、MANUAL.md 1.2 に節がない。
他の行と同じ形式で1節足す。

## 完了

- モックEPGStationに、ルールに紐づかない録画6件を追加（`dataset.js`。モックは Git 管理外）
- そのサムネイルを生成
- `screenshots/row-manual-recorded.png` を撮影（Google TV Streamer / 1920x1080）
- MANUAL.md 1.2 に `#### 番組表からの録画` を追加

## 残タスク

- ユーザーによる記述の確認
- WIP.md を削除してから PR 作成

## 決定事項

- 節の位置は「最近の録画」と「検索履歴」の間（アプリの行の並びと同じ）
- 集める範囲の上限（新しい録画3000件）は利用者から見える制約なので明記した
- 6件のサムネイルは背景画なしの単色。ComfyUI が停止中で `art_<作品名>.jpg` を作れなかったため
