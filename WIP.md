# WIP: 先頭の『』をシリーズ名として取り出す

ブランチ: `fix/series-title-quoted-work-name`

## 目的

`TVアニメ『作品名』#8` / `TVアニメ『作品名』第8話` 形式の番組名で、シリーズ名が
「TVアニメ」と判定される誤検出を直す。`『` が境界記号(A4)に入っているため、
ジャンルの名乗りの直後で切れてしまっていた。別作品どうしが同じシリーズに
まとめられるので実害が大きい。

## 完了済み

- `SeriesTitleExtractor` に前段の規則を追加。行頭タグを落とした後の**先頭**に
  `(ジャンルの名乗り)?『…』` があれば、中身をシリーズ名として返す。
  - 名乗りの辞書 `PREFIXES` は実データで確認できた `TVアニメ|ドラマ|夕方ミステリー` の3語のみ。
  - 名乗りは省略可。行頭がいきなり `『` の番組名も対象になる。
- golden set にセクション「先頭の『』」を追加（9件。うち3件は切ってはいけない側・辞書に無い側の確認）。
- 検証用サーバーの録画456本で新旧の抽出結果を突き合わせ済み。差分は6件で全て改善、
  それ以外の450本は完全に一致。`番組名 #7「AとB『C』」`（文中の『』）も従来どおり。

## 残タスク

- ユーザーによる実機での動作確認。
- 確認が取れたら WIP.md を削除してコミット → PR 作成。

## 決定事項

- **先頭に限る。** 文中の『』は作品名とは限らない（副題の中の引用、番組名に続く企画名）。
  そこは境界記号として切るのが正しいので A4 のまま。
- **中身に区切り規則を再適用しない。** 『』は放送局自身が付けた作品名の囲みなので、
  中身はそのまま採る。
- **名乗りは辞書型にする。** 実在の番組名で確認できた語だけを並べる方針
  （タグ辞書と同じ考え方）。翌週の番組表を API で走査した結果は下記の「未決」に置く。

## 未決（ユーザー判断待ち）

翌週の番組表（全11788番組、うち『を含むもの148件）を走査したところ、`『` が最初の区切りに
なる番組名の頭は29種あった。辞書に足す価値があるのは枠・ジャンルの名乗りだけで、
`劇団朱雀` `岸辺露伴は動かない` `イベントGO!` のように「切るのが正しい」ものも混ざっている。
候補（件数）: ザ・ミステリー(5) / 土曜お昼のミステリー(1) / 傑作ミステリー(1) /
韓国ドラマ(1) / ミュージカル(1) / 劇場版(1) / アニメ(1) / 特別番組(1)
