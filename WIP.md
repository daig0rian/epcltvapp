# WIP: 録画ルールの並び順に合わせたドキュメント更新

ブランチ: `docs/rule-order-manual`（master 015e93a から分岐）

## 目的

PR #92「録画ルール一覧を『最後に録画された順』で並べられるようにする」がマージされ、
設定画面の構成が変わった。MANUAL.md の記述が事実と食い違っているので直す。
あわせて次回リリースのノートに、既定の並び順が変わることを書く。

## 背景: 設定画面はこう変わった

| カテゴリ | #92 以前 | 現在 |
|---|---|---|
| 全体設定 | サムネイル背景表示 / ルール・履歴の新しい順表示 | サムネイル背景表示のみ |
| ルール表示設定 | 0件ルールの表示 | **ルールの並び順（3択）** + 0件ルールの表示 |
| 履歴表示設定 | 表示件数 / 履歴のクリア | **履歴の新しい順表示** + 表示件数 / 履歴のクリア |

「ルールの並び順」の3択は、ルールの新しい順 / ルールの古い順 / 録画の新しい順。既定は
**ルールの新しい順**。

## 残タスク

- [x] MANUAL.md:415 の修正（「全体設定」の説明が嘘になっている。ルール/履歴の新しい順表示は
      全体設定から消えた）
- [x] MANUAL.md:416 への追記（ルール表示設定に並び順の3択が増えた）
- [x] MANUAL.md:417 への追記（履歴表示設定に履歴の新しい順表示が移ってきた）
- [x] MANUAL.md:94 への追記（録画ルールの行。目玉である「録画の新しい順」に触れる）
- [x] `screenshots/settings-display.png` の撮り直し（Google TV Streamer + mock-epgstation）
- [ ] ユーザーによる内容確認
- [ ] 次回リリースノートに既定の並び順変更の告知 ← **このブランチには含めない**（下記）

## リリースノートをこのブランチに含めない理由

CLAUDE.md のリリースフローでは、リリースノートは `versionCode` / `versionName` の更新とセットで
**master に直接コミット**する扱いで、かつ**ユーザーの承認が出るまでコミットしない**。docs ブランチに
先に入れるとこの手順と噛み合わないため、リリース時に対応する。忘れないよう Issue 化するかどうかは
ユーザーの判断。

## 重要な決定事項

- **既定の並び順は PR のままにする。** 旧設定からの移行はしない。設定を一度も触っていない
  利用者は「古いルールが上」→「新しいルールが上」へ反転するが、リリースノートで案内する
  方針をユーザーが選択済み。
- **リリースノートでの告知には穴がある。** アプリ内アップデートが見るのは GitHub の
  `releases/latest` だけなので、1つ飛ばして更新した利用者には届かない。告知を入れたリリースの
  次の版にも一言残すかどうかは、リリース時に判断する。
- **スクリーンショットは Google TV Streamer で撮る。** 既存のスクリーンショットが一貫して
  この端末で撮られているため。`screenshots/README.md` のとおり `mock-epgstation/` に接続して
  撮影し、実在の番組名が写り込まないようにする。
- **release-notes/*.md の過去分は直さない。** 各リリース時点の記録なので、直すと履歴が歪む。

## 前提

- `screenshots/settings-display.png` の撮影には、Google TV Streamer に #92 を含む master の
  ビルドが入っている必要がある。2026-09-18 時点では Fire TV にしか入れていない。
