# WIP: シリーズ連続再生 (feature/series-autoplay)

## 目的

再生が終端に達したあとプレーヤー画面のまま止まる挙動をやめ、

1. 同じシリーズの次の録画へ自動的に進む（設定でON/OFF・既定ON）
2. 自動再生がOFF、またはシリーズの末尾・シリーズなしのときは詳細画面へ戻る
3. 再生画面に「前トラック／次トラック」ボタンを置き、手動でも前後の回へ移動できる

ようにする。

## 決めたこと（ユーザーと合意済み）

- **シリーズの前後の判定**: 話数表記（#2 の次は #3 など）は見ない。`SeriesTitleExtractor` で
  取り出したシリーズ名で録画を検索し、**録画開始時刻順に並べた隣**を前後の回とする。
- **番組名で絞り込む**: EPGStation のキーワード検索は番組名だけでなく番組概要にもマッチする
  (`RecordedDB.ts` の `halfWidthName OR halfWidthDescription`)。概要だけが一致した無関係な
  番組へ自動で飛ばないよう、番組名にシリーズ名を含むものだけを対象にする。
- **設定の既定値**: ON。設定 > プレーヤー に置き、内蔵プレーヤー選択時のみ表示（レジューム再生と同じ）。
- **遷移の見せ方**: 即座に切り替え＋「次: <番組名>」の短いトースト。
- **前/次ボタン**: 録画再生中は常に表示。前トラックボタンは音楽プレーヤーと同じ挙動にする
  ——冒頭（5秒未満）なら前の回へ、それ以外はその回の先頭へ戻る。
- **戻り先の詳細画面**: 自動再生で番組が変わっている場合、戻るボタン・再生終了で戻る詳細画面は
  **今見ていた番組**のものに差し替える（`FLAG_ACTIVITY_CLEAR_TOP` でスタック上の詳細画面を置換）。
- **対象外**: ライブ視聴（mpegts直送・HLS）と、収録中TSのHLS追いかけ再生。従来どおり終端で何もしない。

## 実装方針

- **番組の切り替えは PlaybackActivity 内でフラグメントを作り直す。** 先に今のフラグメントを
  `commitNow()` で同期的に破棄してから次を追加する。ExoPlayer の解放が次のプレーヤー生成より
  先に済むことを保証するため（デコーダーのインスタンスが同時に2つ開くと機種によってはハングする。
  CLAUDE.md「TSシークバーのサムネイル表示」参照）。
- 次の回で再生するファイルは、今再生しているものと同じ種類・同じ名前を優先して選ぶ
  （TSを見ていたら次もTS、"720p" を見ていたら次も "720p"）。

## 実機フィードバックを受けた2巡目（2026-08-15）

1巡目（自動再生・前/次トラック）は意図どおり動作。ここからは操作性の課題を4点直す。

- **① ボタンにキャプションを出す。** アイコンだけでは何のボタンか分からない。フォーカス中の
  ボタンの真下にだけ名前を出す（全ボタンに常時出すとキャプションが重ならないようボタン間隔を
  大きく空ける必要があり、ボタン列が間延びするため）。キャプション行はボタン列とシークバーの
  間に1行分を常に確保して置く。
- **② 「最初から再生」と「前のエピソード」をボタンとして分ける。** 音楽プレーヤーの慣例
  （再生位置で動作を変える）は実機では使いにくかった。
- **③ 「他のエピソード」ボタンとエピソード一覧の行を追加する。** 一覧は常にコントロール行の
  下に置き、ボタンはそこへフォーカスを移すショートカットにする。
- **④ コントロールを開いたときのフォーカスを再生/一時停止ボタンにする。**
  → **3巡目で方針転換。** 下記参照。

## 実機フィードバックを受けた3巡目（2026-08-16）

④の「開いたら再生/一時停止ボタンにフォーカス」は取り下げ、**Leanback の既定（シークバーに
フォーカス）に戻す**。閉じている状態からの左右キーは常に早送り/巻き戻しであってほしく、
その意味で既定の挙動のほうが正しいため。実機では次の3通りに割れていた。

- 再生直後に開く → シークバー（既定のまま）
- 再生中にDPADで開く → 再生/一時停止ボタン（④の実装）
- エピソード一覧を見た状態で閉じてから開く → エピソード一覧（Leanback が最後の位置を覚えている）

直すのは次の2点。

- **並び順を実際のフォーカス位置に合わせる。** コントロール行を
  **[シークバー][時刻][ボタン列][キャプション]** に組み替える（Leanback の既定は
  [ボタン列][シークバー][時刻]）。下からせり上がってくるコントロールの一番上にボタン列が
  あると、そこにフォーカスがあるつもりで操作してしまうため。
- **開くたびに毎回シークバーへフォーカスを揃える。** 閉じる直前の位置は引き継がない
  （エピソード一覧まで降りていた場合に備えて行の選択位置も0へ戻す）。

### Leanback 側の制約（調べた結果）

- **初期フォーカスはシークバーへ吸われる。** `PlaybackTransportRowView.onRequestFocusInDescendants`
  が行内に `playback_progress` があれば必ずそれを選び、`PlaybackTransportRowPresenter.onReappear`
  も `mProgressBar.requestFocus()` を呼ぶ。→ 公開APIの `showControlsOverlay(boolean)` を
  override し、閉じている状態から開いたときだけ先頭ボタンへフォーカスを移して対処。
  なお `tickle()`（DPAD操作でコントロールを開く経路）はこの公開APIを呼ぶので確実に通る。
- **ボタンのキャプション表示はLeanbackに無い。** `ControlButtonPresenterSelector` はアイコンが
  無いときだけボタン中央にラベルを描く。ただしどのボタンも `contentDescription` に
  `Action` のラベルが入るので、フォーカス中のビューからそれを読んでキャプションに出せる。
- **コントロール行の下に行を並べられる。** `PlaybackSupportFragment.setAdapter()` に
  `ArrayObjectAdapter` を渡すと、グルーが0番目に再生コントロール行を入れる仕様
  （`setupRow()`）。エピソード一覧はその後ろに足す。

## 実装中に判明したこと

- **コントロール行の初期フォーカスは常に左端のボタン。** `PlaybackTransportRowPresenter` が
  `ControlBar.setDefaultFocusToMiddle(false)` を指定しており、既定のフォーカス位置は0番目で固定
  （`ControlBar.getDefaultFocusIndex()`）。前トラックを左端に置くと、コントロールを開いた直後の
  OK が一時停止ではなく頭出しになってしまうため、並びは
  **[再生/一時停止][前][次][CC][SI][音声]** にした（左端は従来どおり再生/一時停止）。

## 完了済み

- [x] `SeriesPlaylist.kt` 新規作成（シリーズ検索・並べ替え・前後の解決・再生対象の選択）
- [x] `SeriesPlaylistTest.kt` 新規作成（並び順・番組名フィルタ・再生対象選択の回帰テスト）
- [x] `PlaybackVideoFragment` 改修（終端検知・前/次トラック・番組切り替え）
- [x] `PlaybackActivity` 改修（フラグメント作り直し・戻り先の詳細画面差し替え）
- [x] 設定項目の追加（`preferences.xml` / `strings.xml` / `SettingsFragment`）

## 完了済み（2巡目）

- [x] ① フォーカス中のボタンのキャプション表示（ボタン列とシークバーの間に1行）
- [x] ② 「最初から再生」ボタンを分離（前トラックは前のエピソードのみ）
- [x] ③ 「他のエピソード」ボタンとエピソード一覧の行
- [x] ④ コントロールを開いたときのフォーカス（3巡目でシークバーへ揃える方針に変更）

## 完了済み（3巡目）

- [x] コントロール行を [シークバー][時刻][ボタン列][キャプション] の順に組み替え
- [x] コントロールを開くたびにフォーカスをシークバーへ戻す（行の選択位置も0へ）
- [x] 並べ替えでフォーカスが落ちる問題の修正（再生開始直後だけ再生ボタンに当たっていた）
- [x] エピソード一覧のカーソルも、コントロールを開くたびに今見ている回へ戻す
      （一覧で選んでいた位置は覚えない）

## 完了済み（4巡目）

- [x] 「前のエピソード」「他のエピソード」ボタンを削除（一覧が常に見えているので不要）
- [x] ボタン間隔を詰めた（Leanback の `lb_playback_controls_child_margin_default` を
      48dp → 32dp に上書き。中心間 80dp → 64dp）
- [x] 一時停止したときにキャプションが「一時停止」のままだったのを直した
      （再生/一時停止は Leanback 自身がラベルを差し替えるため、押した経路ではなく
      アクションのアダプタの通知を見てキャプションを追従させる）
- [x] キャプションの意味を全ボタンで「押した後にどうなるか」に統一
      （字幕・文字スーパー・音声は今の状態を出していた）

ボタンの並びは **[再生/一時停止][最初から再生][次のエピソード][字幕][文字スーパー][音声]**。

## 残タスク

- [ ] ユーザーによるビルド（Android Studio）と `SeriesPlaylistTest` の実行
- [ ] 実機動作確認
  - 再生終了 → 次の回へ自動で移る（トースト「次: …」）
  - 設定OFF・シリーズ末尾・シリーズなし → 詳細画面へ戻る
  - 「次のエピソード」ボタンで次の回へ、「最初から再生」で頭出し
  - ボタン間隔が詰まっていて、フォーカス時の円が隣のアイコンにかかっていない
  - フォーカスを動かすと、そのボタンの真下にキャプションが出る（字幕・音声はON/OFFで文字が変わる）
  - 「他のエピソード」ボタンで一覧へ移動し、今見ている回にカーソルが合っている
  - 一覧のカードを選ぶとその回が再生される
  - コントロールが上から [シークバー][時刻][ボタン列][キャプション] の順に並んでいる
  - コントロールを開いた直後は**常に**シークバーにフォーカスがあり、直前にエピソード一覧や
    ボタンを触っていても引き継がれない（閉じた状態からの左右キーが常に早送り/巻き戻しになる）
  - 自動で数話進んだ後の「戻る」で、今見ていた回の詳細画面が出る
  - ライブ視聴・収録中TSの追いかけ再生が従来どおりであること（終端で何も起きない）
