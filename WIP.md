# WIP: 先頭の『』をシリーズ名として取り出す

ブランチ: `fix/series-title-quoted-work-name`

## 目的

`TVアニメ『作品名』#8` / `TVアニメ『作品名』第8話` 形式の番組名で、シリーズ名が
「TVアニメ」と判定される誤検出を直す。`『` が境界記号(A4)に入っているため、
ジャンルの名乗りの直後で切れてしまっていた。別作品どうしが同じシリーズに
まとめられるので実害が大きい。

## 完了済み

- `SeriesTitleExtractor` に前段の規則を追加。行頭タグを落とした後の**先頭**に
  `(ジャンルの名乗り)?『…』` があれば、中身をシリーズ名として返す。
  - 名乗りの辞書 `PREFIXES` は `TVアニメ|アニメ|ドラマ` の3語。
  - 名乗りは省略可。行頭がいきなり `『` の番組名も対象になる。
- golden set にセクション「先頭の『』」を追加（10件。うち3件は切ってはいけない側・
  辞書に無い側の確認）。全108件で不一致 0。
- 実データで新旧の抽出結果を突き合わせ済み。
  - 録画済み456本 … 差分6件・すべて改善。残り450本は完全一致。
  - 翌週の番組表11788本 … 差分30件・すべて改善。
  - `番組名 #7「AとB『C』」`（文中の『』）は従来どおり切らない。

## 残タスク

- ユーザーによる実機での動作確認。
- 確認が取れたら WIP.md を削除してコミット → PR 作成。

## 決定事項

- **先頭に限る。** 文中の『』は作品名とは限らない（副題の中の引用、番組名に続く企画名）。
  そこは境界記号として切るのが正しいので A4 のまま。
- **中身に区切り規則を再適用しない。** 『』は放送局自身が付けた作品名の囲みなので、
  中身はそのまま採る。
- **名乗りは辞書型にする。** 正規表現でジャンル語をまとめて拾う書き方は範囲が広すぎた。
  タグ辞書と同じく、実在の番組名から拾った語をそのまま並べる。

### 辞書に載せる基準（ユーザー判断）

**一般名詞でジャンルを示す語だけを載せる。固有名詞の枠名は載せない。**

翌週の番組表を走査して候補を検討した結果、除外したものと理由:

| 候補 | 判断 |
|---|---|
| 曜日・時間帯を冠したミステリー枠 4語 | 固有名詞の枠名なので載せない。一般名詞だけの `ミステリー『』` は実在しなかったので「ミステリー」も不採用 |
| 「(国名)ドラマ」 | 同上の流れで不採用 |
| ミュージカル | 実例が少なすぎて判断できない。保留 |
| 劇場版 | 劇場版は1話・2話と続くシリーズになりにくい。保留 |
| 特別番組 | そもそもシリーズになりにくい。別問題 |

`アニメ` は基準に照らして採用（実例は `アニメ 『作品名』 シーズンN` の形で、名乗りと
『』の間に空白が入る）。

なお候補の検討にあたって放送局ごとの分布も調べたが、**局名・実在の番組名はこのファイルに
書かない**（→ CLAUDE.md「成果物に実在の番組を持ち込まない」）。

## 後退試験の結果

旧実装(前段規則なし)と新実装を全件で突き合わせた。判定は `SeriesPlaylist.matchesSeries`
と同じ NFKC 部分一致で行っている。

| 見たもの | 録画済一覧 456本 | 番組表 11788本 |
|---|---|---|
| 変化なし | 450 | 11758 |
| 変化あり | 6 | 30 |
| シリーズ名が空になった | 0 | 0 |
| 自分自身の番組名に当たらなくなった | 0 | 0 |
| 仲間を失った(別作品が外れた分を除く) | 0 | 0 |

変化した36件はすべてグルーピングの改善だった。名乗りだけが残っていたときのグループは
番組表上で「ドラマ」423本・「アニメ」128本という規模で、これが各作品に分かれた。

シリーズ名が短くなることで無関係な番組を巻き込む例が1件あるが、**同じ名前の番組が
元から持っていた挙動と同じになっただけ**で、この変更が作り出したものではない。

## 別件として見つかったもの: 名乗り + 「」

名乗りの後ろが `『』` ではなく `「」` の番組が番組表に13件あり、こちらは今も名乗りだけが
シリーズ名として残る(同じシリーズの5話分がまとめて巻き込まれている例もある)。同じ誤判定の
別表記版。`「」` は各回の副題にも使われるため、**名乗りを必須にする**(行頭がいきなり `「` の
場合は対象にしない)なら同じ仕組みで解ける。着手はユーザーの判断待ち。

## 追加対応: 名乗り + 「」（対応済み）

上の「別件」をこのブランチで取り込んだ。前段の規則を2本にして、囲みごとに名乗りの
必須／省略可を変えている。

| 囲み | 名乗り | 理由 |
|---|---|---|
| `『』` | 省略可 | 作品名を囲む記号。行頭に置かれていれば中身が作品名 |
| `「」` | **必須** | 各回の副題にも使う記号。行頭がいきなり `「` の番組名まで拾うと副題を作品名と取り違える |

golden set は「」側を5件足して全113件・不一致0。後退試験もやり直した。

| 見たもの | 録画済一覧 456本 | 番組表 11788本 |
|---|---|---|
| 変化なし | 450 | 11746 |
| 変化あり | 6 | 42 (『』30 + 「」12) |
| シリーズ名が空になった | 0 | 0 |
| 自分自身の番組名に当たらなくなった | 0 | 0 |

名乗りだけがシリーズ名として残る番組は、録画済一覧で0件、番組表で1件になった。
残る1件は囲みを持たない書き方(`アニメ (作品名) 08 (副題)`)で、この規則では扱えない。
