# WIP: 初回起動時に設定画面が自動で開くようにする

Issue #52。最下段の設定行を引退させる前提として先に直す。

## 目的

初回起動で接続先が未設定のとき、設定画面が自動で開くようにする。

いまは開かない。`MainFragment.onCreate()` が `PreferenceManager.setDefaultValues()` を
呼んだ直後に `isPreferenceAllExists()` を評価しており、`setDefaultValues()` が
`preferences.xml` の `defaultValue` を書き込むため、初回起動の時点で
`KEY_IP_ADDR = 192.168.0.0` / `KEY_PORT_NUM = 8888` が入る。どちらも正規表現を通るので
「設定済み」と判定されてしまう。v1.30 (#18) で `setDefaultValues()` が入って以降の挙動。

## なぜ引退より先か

最下段の設定行を消すと、接続に失敗した初回起動の利用者は**行が1つも無いメイン画面**を
見ることになる。#98 の歯車はサイドバーの帯が来ているときだけ出るため、その状態では
歯車も出ない。設定への入口が画面上に1つも無くなり、再インストール以外に復旧手段が
なくなる恐れがある。

いまは接続の成否に関係なく設定行だけは出る（`loadRows()` の `addToCategory`）ので詰まない。

## 方針

Issue の対応案のうち**「既定値のままなら未設定とみなす」**を採る。

- 案2（`defaultValue` を空にする）は、既にプレースホルダが書き込まれた端末を救えない
- 案3（初回起動フラグ）は台帳が増える

`192.168.0.0` はネットワークアドレスでホストアドレスとして成立しないので、
これを「未設定」とみなして誤判定になる場面はない。判定はリテラルではなく
`R.string.pref_val_ip_addr_default` と比べる（既定値を変えたら判定も追従するように）。

ポート既定値の `8888` は EPGStation の正規の既定値なので判定に含めない。

## 追加でやること（同じPRにまとめる）

Issue #52 を直した結果、**接続先が未設定のときは設定行も出なくなった**（未設定だと
`onCreate` が設定画面を開くだけで `loadRows()` へ進まないため）。どのみち消す行なので、
引退まで同じPRでやる。

- [x] `Category.SETTINGS` と `loadRows()` の行構築を削除
- [x] クリック処理の `SettingsCardPresenter.Item` 分岐を削除
- [x] `SettingsCardPresenter.kt` を削除（他から使われていない）
- [x] `mSettingsRowAdapter`、サイドバーのアイコン対応表の該当行を削除
- [x] MANUAL.md の記述を歯車からの導線に書き換え

文字列・drawable はすべて `preferences.xml`（#105）が使っているので、未使用になったものは無い。

## 残タスク

- [x] `isPreferenceAllExists()` に既定値の判定を足す
- [ ] 行がゼロのときに歯車が出るかの確認（出ないなら手当てが要る）
- [ ] screenshots/setup-1-settings-row.png と row-settings.png の撮り直し
- [ ] 実機確認
- [ ] WIP.md を削除してコミットしてから PR 作成

## 実機確認の観点

- アプリのデータを消して起動し、設定画面が自動で開くこと
- 接続先を設定して戻ると、通常どおり一覧が読み込まれること
- 既に正しい接続先が入っている端末では、設定画面が開かないこと（従来どおり起動する）
- 「API Base URLを編集する」を使っている場合の判定が変わっていないこと
