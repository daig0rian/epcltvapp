# WIP: レジューム再生 (feature/resume-playback)

## 目的

前回途中で再生を止めた録画を再度開いたとき、画面右下に半透明の小さなダイアログを出して
「前回再生を停止した位置から再開しますか？」を確認する。
再生自体は選択によらず最初から開始する（ダイアログの裏で再生が始まっている）。

## 仕様

- ダイアログ
  - 画面右下（端から48dp）、パディング少なめ、半透明（背景の減光なし）
  - 選択肢は [このまま] [前回停止位置から再生]。初期フォーカスは後者
  - チェックボックス「次から表示しない」をメッセージと選択肢の間に置く
  - **一度も操作されなければ20秒で自動的に消える**（=「このまま」と同じ扱い）。残り秒数を表示する。
    **最初のキー操作でカウントダウンは解除**し、以降は時間切れで閉じない（一般的な自動クローズUIに合わせる）。
    キー検出は `Dialog.setOnKeyListener`（全キーをビュー階層より先に拾える）
  - 自動で消えたときはチェックボックスを反映しない（設定変更は明示的なボタン操作のときだけ）
- 設定「レジューム再生」(再生設定・内蔵プレーヤー選択時のみ表示) は3択の ListPreference
  - `毎回確認`(ask, デフォルト) / `最初から`(beginning) / `前回停止位置から`(resume)
  - チェックボックス付きで [このまま] → `beginning`、[前回停止位置から再生] → `resume` を書き込む
  - キーは `KEY_RESUME_PLAYBACK_MODE`（旧 `KEY_RESUME_PLAYBACK` は Boolean だったため型衝突を避けて改名）

## 実装方針

- `PlaybackPositionStore` … 専用 SharedPreferences (`playback_positions`) に
  「動画キー -> 位置ms,更新時刻ms」を保存。件数上限を超えたら更新の古い順に捨てる。
  - キー: v1 は `v1:<recordedProgramId>:<actionId>` / v2 は `v2:<recordedItemId>:<actionId>`
- 保存タイミングは `PlaybackVideoFragment.onPause()`。
  - 10秒未満 → 何もしない（前回位置を消さない。ダイアログを見てすぐ戻った場合の保護）
  - 終端付近(残り15秒以内) → エントリ削除（最後まで見たので次は最初から）
  - それ以外 → 保存
- 位置の記録は設定値によらず常に行う（設定が決めるのは記録の使い方だけ）。
- 対象外: ライブ(HLS/mpegts)、HLS追いかけ再生(`IS_HLS`)。
- TSは `TsSeekDataProvider` の完成待ちが要るため `pendingTsResumePositionMs` に積んで
  `refreshTailAndSeekBar` 完了時に `performTsSeek()` する。

## 完了済み

- 初版実装（51a5207）／マニュアル追記（2cf407b）
- 右下表示・自動クローズ・カウントダウン・選択肢の文言変更・
  「次から表示しない」チェックボックス・設定の3択化（8b0524b）
- 自動クローズを20秒に延長し、最初の操作で解除する方式に変更

## 残タスク

- 実機での動作確認
- `screenshots/settings-playback.png` の撮り直しとダイアログのスクリーンショット追加（任意）
