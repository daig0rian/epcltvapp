# WIP: 再生速度ボタン (feature/playback-speed)

## 目的

再生コントロールに再生速度ボタンを追加する。速度は 1.00x / 1.25x / 1.50x / 2.00x の4段階。

## 決定事項（ユーザー合意済み）

- **ライブ視聴では出さない。** 先頭に追いつけないため速度を上げる意味がない。追いかけ再生には出す。
- **速度は保持しない。** 再生を始めるたびに 1.00x へ戻る（シリーズの次の回へ移ったときも同じ。
  フラグメントが作り直されるので実装上も自然に戻る）。
- **押す→上下キーで選択→もう一度押して確定**。ボタンの上に一覧を出す。
  （最初は押すたびに巡回する案で作ったが、実機で使いにくかったため作り替えた）
- アイコンは Material Symbols の `speed`（速度計）を**そのまま1枚**使う。針を動かす細工はしない。
  等速のみ白・それ以外は水色（既存の「白=OFF / 水色=ON」に合わせる）。
- **ボタンのキャプションは「速度変更」**。他のボタンと同じく「そのボタンが何をするか」に揃える。
  今どの速度かはアイコンの色（等速のみ白）と、確定時の短いトーストで知らせる。

## 実装メモ

- **ボタンは7個が上限。** Leanback の `ControlBarPresenter.MAX_CONTROLS = 7` で8個目以降は
  無言で描画されない。速度ボタンを足すと最大構成（録画TS＋ネイティブTS処理＋シリーズあり）で
  ちょうど7個になり、余白がなくなる。次にボタンを足すときは畳む設計が必要。
- **音声パススルー時は速度指定が効かない。** media3 の `DefaultAudioSink` は
  パススルー/オフロード/トンネリング出力のとき速度調整を諦めて等速に戻す
  (`shouldApplyAudioProcessorPlaybackParameters`)。ボタンだけ速くなったように見えるのを防ぐため、
  `onPlaybackParametersChanged` で実効値を見て、要求と違ったら等速へ戻してメッセージを出す。

## 一覧UIの作り

- 一覧 (`PlaybackSpeedPickerView`) は**フォーカスを取らない**。フォーカスは速度ボタンに置いたまま、
  上下キーをグルーの `onKey` で横取りしてカーソルを動かす。フォーカスを渡すと Leanback の
  フォーカス管理（コントロール行内の移動・行の選択位置）と二重になり、閉じた後の戻り先が
  定まらなくなるため。
- 開いている間は自動非表示を止める（`applyControlsAutoHide` のマスクに `speedPickerOpen` を追加）。
- **戻るキーは Leanback が必ずコントロールを閉じるのに使う**（`onInterceptInputEvent` が
  キーハンドラの結果によらず BACK を消費する）。そのため `hideControlsOverlay` を override して
  一覧も畳む＝戻るキーは「選ぶのをやめる」として働く。

## 完了済み

- 実装一式（アイコン・文言・一覧UI・キー処理・速度適用・効かなかった場合の受け止め）
- MANUAL.md 4.2 の更新

## 残タスク

- ユーザーの実機動作確認 → WIP.md 削除してコミット → PR

## 動作確認の観点

- 一覧が速度ボタンの真上に出るか。上下で選べ、決定で確定し、戻るでやめられるか。
- 上下キーでエピソード一覧へ降りてしまわないか（一覧を開いている間だけ横取りしている）。
- エンコード済み動画で 1.25/1.5/2.0 が効くか。音程が保たれるか。
- **TSの2倍速**（帯域とMPEG-2デコード負荷が2倍になる。Fire TV 要注意）。字幕・文字スーパーが
  速度に追従するか。
- ライブ視聴でボタンが出ないこと。
- 番組を変えると等速に戻ること（シリーズの次の回でも）。
