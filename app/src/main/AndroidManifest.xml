<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- アプリ内アップデートで、落としたAPKをシステムのインストーラーへ渡すために要る。
         宣言しただけでは足りず、利用者が「不明なアプリのインストール」を許可する必要がある
         (API 26 以降。それ以前は端末全体の「提供元不明のアプリ」設定に従う)。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />


    <application
        android:name=".EpgTvApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epcltvapp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:banner="@drawable/ic_banner"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:logo="@drawable/ic_banner"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 外部からライブ視聴を開始するための受け口。
             epcltvapp://live/channelId/<channelId>
             識別子の種別をパスに含めているのは、将来 remoteControlKeyId のような別の
             指定方法を足せるようにするため。 -->
        <activity
            android:name=".DeepLinkActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.Epcltvapp.DeepLink"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="epcltvapp" android:host="live" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailsActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".PlaybackActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".SearchActivity"
            android:theme="@style/CustomizedSearch"
            android:screenOrientation="landscape"/>
        <activity android:name=".SettingsActivity"
            android:theme="@style/LeanbackPreferences"/>

        <!-- アプリ内アップデートで落としたAPKをインストーラーへ渡すための受け渡し口。
             API 24 以降は file:// を他アプリへ渡せないため content:// にする。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>